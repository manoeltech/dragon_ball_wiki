<!DOCTYPE html>
<html>
  <head>
    <title>DragonBallWiki</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <main class="container mx-auto mt-28 px-5 flex">
      <div class="navbar navbar-fixed-top">
        <div class="navbar-inner">
          <div class="container">
            <p class="navbar-text pull-right">
              <% if user_admin_signed_in? %>
                Logged in as <strong><%= current_user_admin.email %></strong>.
                <%= link_to 'Edit profile', edit_user_admin_registration_path, :class => 'navbar-link' %> |
                <%= link_to "Logout", destroy_user_admin_session_path, method: :delete, :class => 'navbar-link'  %>
              <% else %>
                <%= link_to "Sign up", new_user_admin_registration_path, :class => 'navbar-link'  %> |
                <%= link_to "Login", new_user_admin_session_path, :class => 'navbar-link'  %>
              <% end %>
            </p>
          </div>
        </div>
      </div>
      <%= yield %>
    </main>
  </body>
</html>
